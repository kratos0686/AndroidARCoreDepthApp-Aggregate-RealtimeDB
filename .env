# ============================================================================
# SECURITY NOTICE: AI Features Disabled by Default
# ============================================================================
# 
# Gemini API integration has been DISABLED in this repository to prevent
# accidental exposure of API keys or credentials in version control.
#
# DO NOT add API keys or secrets to this file or any tracked files.
#
# ============================================================================
# To Re-Enable AI Features Securely:
# ============================================================================
#
# Option 1: Use a Backend Proxy (RECOMMENDED for production)
#   - Create a secure backend service that handles Gemini API calls
#   - Your app sends requests to your backend, not directly to Gemini
#   - Backend manages API keys securely (environment variables, secret manager)
#   - Benefits: Rate limiting, request logging, cost control, key rotation
#
# Option 2: CI/CD Secret Injection (for development/testing)
#   - Store API keys in your CI/CD platform's secret manager
#     (GitHub Secrets, GitLab CI/CD Variables, etc.)
#   - Inject secrets at build time via environment variables
#   - Never commit the injected values to version control
#
# Option 3: Local Development Only
#   - Create a .env.local file (add to .gitignore)
#   - Add your API key: GEMINI_API_KEY=your_key_here
#   - Load .env.local in your app initialization
#   - Ensure .env.local is NEVER committed
#
# ============================================================================
# Current Status:
# ============================================================================
# - GeminiService: DISABLED (throws exception on initialize)
# - IICRCAssistantService: DISABLED (throws exception on initialize)
# - To enable: Remove exception stubs and provide secure key management
#
# Get API keys from: https://makersuite.google.com/app/apikey
# ============================================================================
